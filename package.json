{
  "name": "explore-nestedset",
  "version": "1.0.0",
  "description": "Fix `left` and `right` values of nestedset model in Juno Dabatase",
  "main": "fix.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
